## Lab Summary

The hardest part of this lab for me wasn't really the JSON file itself — it was figuring out why the filesystem server kept failing even though I'd copied everything correctly. Turns out the folder I pointed it to didn't exist yet, so it had nothing to connect to. Once I created the folder and tested the command directly in the Terminal first, everything clicked into place. Next time I'll test in the Terminal before restarting the whole app, since it's a lot faster to see what's actually going wrong.
